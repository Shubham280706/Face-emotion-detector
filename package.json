{
  "name": "face-emotion-detector-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && cd backend && source .venv/bin/activate && pip install -r requirements.txt",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && source .venv/bin/activate && uvicorn main:app --reload --port 8000",
    "dev": "npm run dev:frontend & npm run dev:backend",
    "build": "npm run build --prefix frontend"
  },
  "description": "Root management for Face Emotion Detector"
}
